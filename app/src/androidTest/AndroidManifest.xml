<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!--
            Required by createAndroidComposeRule<HiltTestActivity>().
            HiltTestActivity is only present in the androidTest build variant;
            this manifest merges into the test APK, not the release APK.
        -->
        <activity
            android:name=".HiltTestActivity"
            android:exported="false" />
    </application>
</manifest>