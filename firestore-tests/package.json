{
  "name": "cybershield-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Unit tests for firestore.rules, run against the Firestore emulator via @firebase/rules-unit-testing. Separate from functions/ (which unit-tests Cloud Functions logic with the Admin SDK) because these tests exercise the security rules themselves, not function code.",
  "type": "module",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.16.0",
    "vitest": "^4.1.10"
  },
  "allowScripts": {
    "protobufjs@7.6.5": true,
    "@firebase/util@1.15.1": true
  }
}
